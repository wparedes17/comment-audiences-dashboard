## Environment
The environment is managed by Conda. The environment name is `fastapi-env` (a general environment I use for several projects and support FastAPI and Flask).

Environment runs over Python 3.11+
